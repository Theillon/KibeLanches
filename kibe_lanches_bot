import telebot

CHAVE_API = "5565456668:AAEiCBJo0XQ1LRpGRwcQDFJ9cxW8SKWQZ4A"

bot = telebot.TeleBot(CHAVE_API)

@bot.message_handler(commands=["Hamburguer"])
def hamburguer(mensagem):
    texto = """
/XTudo - R$ 35,90
/XBurguer - R$ 24,90
/XSalada - R$ 24,90
    """
    bot.send_message(mensagem.chat.id, texto)


@bot.message_handler(commands=["Salgados"])
def salada(mensagem):
    bot.send_message(mensagem.chat.id, """
/Coxinha - R$ 4,90
/Bauru - R$ 5,80
/Kibe - R$ 4,50
     """)


@bot.message_handler(commands=["1"])
def opcao1(mensagem):
    texto = """
O que você quer? (Clique em uma opção)
/Pizza
/Hamburguer
/Salgados
    """
    bot.send_message(mensagem.chat.id, texto)


@bot.message_handler(commands=["2"])
def opcao2(mensagem):
    bot.send_message(mensagem.chat.id, """
Para enviar uma reclamação, sugestão ou elogio, mande um e-mail para suporte@kibelanches.com

Para começar novamente clique em -> /start
    """)


@bot.message_handler(commands=["3"])
def opcao3(mensagem):
    bot.send_message(mensagem.chat.id, """Nossas unidades:
    
Kibe Lanches – Praça da Árvore
Rua Itapiru, 338 - Vila da Saúde, São Paulo – SP

Kibe Lanches – Vila Mariana
Rua Leandro Dupret, 643 – Vila Clementino – SP

Kibe Lanches – Jabaquara
Rua Parnaíba Paolielo, 388 – Jardim Oriental – SP 

Para começar novamente clique em -> /start
""")


@bot.message_handler(commands=["Pizza"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
Escolha o sabor desejado: (Clique no item):
    
/Calabresa - R$ 39,90
/Mussarela - R$ 39,90 
/Portuguesa - R$ 44,90 

Responder qualquer outra coisa não vai funcionar, clique em uma das opções
    """)


@bot.message_handler(commands=["Coxinha"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
        """)


@bot.message_handler(commands=["Bauru"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
            """)


@bot.message_handler(commands=["Kibe"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
            """)


@bot.message_handler(commands=["Xtudo"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
            """)


@bot.message_handler(commands=["Xburguer"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
            """)


@bot.message_handler(commands=["Xsalada"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
            """)


@bot.message_handler(commands=["Calabresa"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
            """)


@bot.message_handler(commands=["Mussarela"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
            """)


@bot.message_handler(commands=["Portuguesa"])
def pizza(mensagem):
    bot.send_message(mensagem.chat.id, """ 
    Insira o endereço:
            """)


@bot.message_handler(commands=["start"])
def responder(mensagem):
    bot.send_message(mensagem.chat.id, """ 
Olá, bem vindo à Kibe Lanches, escolha uma opção abaixo para continuar (Clique no item):

/1 Fazer um pedido
/2 Faça uma reclamação, sugestão ou elogio
/3 Conheça nossas unidades

Responder qualquer outra coisa não vai funcionar, clique em uma das opções
                """)


@bot.message_handler(commands=["cancelar"])
def cancelar(mensagem):
    bot.send_message(mensagem.chat.id, """ 
Seu pedido foi cancelado!

Para começar novamente, clique aqui -> /start
                """)


@bot.message_handler(commands=["Ticket"])
def ticket(mensagem):
    bot.send_message(mensagem.chat.id, """ 
Seu pedido está sendo preparado, e logo será enviado!

Para cancelar, clique aqui -> /cancelar
                    """)


@bot.message_handler(commands=["Credito"])
def ticket(mensagem):
    bot.send_message(mensagem.chat.id, """ 
Seu pedido está sendo preparado, e logo será enviado!

Para cancelar, clique aqui -> /cancelar
                        """)


@bot.message_handler(commands=["Debito"])
def ticket(mensagem):
    bot.send_message(mensagem.chat.id, """ 
Seu pedido está sendo preparado, e logo será enviado!

Para cancelar, clique aqui -> /cancelar
                        """)


def verificar(mensagem):
    return True

@bot.message_handler(func=verificar)
def responder(mensagem):
    texto8 = """
Por favor, selecione a forma de pagamento!
    
/Credito
/Debito    
/Ticket
    
Responder qualquer outra coisa não vai funcionar, clique em uma das opções    
    """
    bot.reply_to(mensagem, texto8)


bot.polling()
